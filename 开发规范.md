# 开发规范
## 中心规则
### **最高优先级规则** 
- 对用户提供的 TODO，**必须**逐句建立可验证的实现与验收对应关系,**必须**自己根据TODO反复逐句修改,
- **绝对禁止**自己随便实现一个长得像TODO但不符合用户的TODO的某一句的事情发生, 每一句用户 TODO 都必须在实现、配置、迁移、测试或验收记录中有明确对应,每一句TODO都要遵守开发规范。
- **绝对禁止**将任何一项降级为 MVP、替代实现或视觉近似, 如无法复用用户指定组件，必须先说明阻塞并征得用户同意。任何关联后端的功能业务修改,都要对前端部分进行完备的同步补充前端 API 客户端请求构造测试,绝对禁止在**纯前端里面塞假数据糊弄用户**或者**放个按钮不接借口**这样的敷衍行为(甚至美其名曰MVP/第一版).
- 禁止治标不治本的偷懒行为,比如遇到存储型问题用bash**注入而不改代码**,比如在**纯前端里面塞假数据糊弄用户**,比如**放个按钮不接借口**,比如直接在代码里面**硬编码**测试用例,或者找到问题**只汇报给用户但是不改**.这属于是绕过问题而不是解决问题,会导致把库删了仍然存不了,属于是**绝对禁止**的行为!!!!!
   **特别针对Codex**: **绝对禁止**那不必要的所谓"MVP"和"第一版""第二版"理念,用户的需求就是天,当用户要求实现一整个功能的时候,应该**反复迭代**,**反复测试**,直到**100**%符合用户的需求! 
- 写完代码**必须**重读一遍*TODO*和*中心规则*,寻找是否存在任何不符合规则的地方,有则必须改正. 写完代码后把自己产生的垃圾文档清理掉.
- 所有AI都应该使用`ponytail`这个skill写代码,这个skill可以帮助减少大量的废代码,并极大地进行代码复用.
- 所有AI都应该使用`design-taste-frontend`这个skill来设计和复审前端的页面编写.这个skill能够帮助页面的去AI化.

### **核心规则**
- **特别针对Codex**:统一采用UTF-8编码,**禁止使用Powershell**,禁止将文件写中文乱码.
- 变更日志规范: 所有AI在执行了代码修改后都必须在`docs/change_history/YYYY-MM-DD.md`对应当天日期文件中书写代码修改日志；当天文件不存在时创建，并同步更新`docs/change_history/README.md`日期索引.(简介为主,简单的UI或者组件小改不用写)
- 测试规范: 是否测试或构建应由修改范围决定,两三个文件不超过100行的小修改,不要运行测试; 纯后端、协议、迁移和内部重构必须执行与改动范围对应的 API、服务、数据或回归验证; 重要的前后端联合功能与交互修改,完成前必须逐项进行实际界面冒烟验证，并报告未完成项。
- 注释规范: 所有AI都应该在任何可以写注释的地方写充分的注释,所有文件无论大小,都要在顶部书写文件功能与使用说明注释,所有的类和字段和函数都要**充分**的写注释来描述功能和使用方法.
- bug探测规范: 任何的代码修改都要遵循**测试寻bug**的原则:
  - 首先找到核心bug的问题所在(用户的反馈,自身的发现);
  - **通过测试确认找到的问题到底是不是问题**(缺陷修复必须先建立能够复现问题的测试);
  - 然后再修,最后自行测试看看**改之后是否解决了问题**,且**没有制造其他的bug**(修复并执行回归验证,重构应验证修改前后行为一致).
- 代码行数规范: 禁止一次性写入超过**1000行**的同一个文件,如果遇到需要写入超过1000行的情形也应该拆成子组件,或者按照领域所在拆成多文件.重构思想为主,满足当前小修改为辅.
- 端口规范: **任何AI,自己开的端口,必须自己关,每次最终回答之前必须关掉自己开的所有端口**.
### Vibe Coding 八荣八耻
以暗猜接口为耻，以认真查阅为荣。  
以模糊执行为耻，以寻求确认为荣。  
以盲想业务为耻，以人类确认为荣。  
以创造接口为耻，以复用现有为荣。  
以跳过验证为耻，以主动测试为荣。  
以破坏架构为耻，以遵循规范为荣。  
以假装理解为耻，以诚实无知为荣。  
以盲目修改为耻，以谨慎重构为荣。
## 前端
1. UX规范: 
   - **强响应式**: 严格贯彻强响应式布局,遵守三级响应式布局(“桌面端 1024px / 平板端 768px / 移动端 480px”,以及超窄模式的一些基本使用考虑),按照交互逻辑重要性与UX进行组件选择性呈现,必要时可按照不同布局研发出不同的组件.涉及响应式布局的部分,必须截图,通过Agent自身的视觉观察通过(布局合理,交互合理,组件无截断或缺失,无莫名空缺)后才能验收.
   - **去AI化**: 去AI化,少用胶囊标签,禁止在大字旁边或者下面放灰色解释性小字,禁止使用一个带有边框或者阴影的圆形或圆角矩形来包裹一个图标按钮(小按钮尽量无边框无阴影),禁止用圆角矩形包裹一行文字和一个数字这种丑陋的数字陈列. 简约为主,实用为主,以提升用户体验为目标,以编写不必要的垃圾文本为耻. 避免一切的AI味组件.
2. 所有组件均需划分为页面组件与子组件,分别放置在`src/views`与`src/components`目录下,页面组件需使用大驼峰命名法,子组件应当存储于相应的页面的蛇形命名法目录下,并使用大驼峰命名法命名组件文件,组件文件内的组件名称也应当与文件名保持一致,如:
```
src/
├── views/                  # 页面组件（路由直接指向的组件）
│   ├── UserProfile.vue     # 大驼峰命名
│   └── HomeView.vue
│
└── components/             # 子组件（被页面或其它组件引用的部分）
    ├── user_profile/       # 对应页面的蛇形命名文件夹
    │   ├── UserInfo.vue    # 大驼峰命名，与文件名一致
    │   └── SecuritySet.vue
    └── common/             # 全局通用组件
        └── CustomButton.vue
```
3. 根据核心规则,组件禁止超过1000行,凡是超过1000行的组件,都应该进行重构,在维持当前样式和业务和交互逻辑完全不变的前提下,按照组件结构独立程度拆分组件.
4. 所有API路由与请求拦截器均登记于`src/router/api_routes.js`文件中,页面路由和路由守卫和vue-router相关的逻辑均登记于`src/router/index.js`中,以便于统一管理和维护。禁止自己乱放路由.
5. 全局统一样式管理:
  - 全局配色切换统一由`src/stores/settings.js`管理: 使用`theme_mode`控制明暗主题,通过给`document.documentElement`设置`data-theme`实现`light`/`dark`/跟随系统切换; 使用`color_scheme`控制全局配色,通过设置`data-color-scheme`在`src/assets/main.css`与`src/assets/ui-system.css`中消费对应主题变量.禁止绕开该机制自行编写非全局配色,一切的配色都要在`settings.js`中登记.
  - 全局字体切换统一由`src/stores/settings.ts`管理: 使用`ui_font_families`控制界面字体家族,使用`text_font_families`控制文字字体家族,通过设置`document.documentElement`上的字体CSS变量在`src/assets/main.css`与`src/assets/ui-system.css`中消费,非必要禁止绕开该机制在组件内硬编码全局字体,原默认字体栈必须保留为fallback.
6. 所有的组件应保持**高可复用性**,组件/布局/样式,写页面和组件的时候应该首先调查前端其他页面和组件,尽量在样式上保持一致.能复用绝不重写,能抽象绝不重复,能组件化绝不冗余.
7. 新增或修改前端调用的后端接口时,必须同步检查并更新`editor/vite.config.ts`中的Vite开发代理配置,确保新增接口前缀能从前端开发端口正确转发到后端;提交前必须通过前端开发端口对新增接口执行一次真实请求验证,禁止只测试后端端口而遗漏开发代理导致404.

注: 以上规则中,`js`可以视情况改为`ts`.


## 后端
1. **持久化红线**: 任何用户级、业务级、跨设备或需要被后端/多端一致读取的数据, 必须通过正式后端模型、服务层、数据库表和迁移脚本持久化, 禁止用浏览器存储(localStorage/sessionStorage/IndexedDB)、前端内存状态、随手 JSON/YAML/文本文件、临时脚本注入等方式冒充业务持久化。只有在数据明确属于纯 UI 临时偏好、可丢弃缓存、调试探针, 或用户明确要求本地一次性保存时, 才允许使用浏览器存储或文件型临时存储; 使用前必须在实现说明中写明原因、生命周期和清理/迁移策略。遇到收藏、会话、用户设置、知识库元数据、权限、同步状态等业务数据时, 默认必须走后端 API + 数据库, 不得擅自降级为前端本地存储或 JSON 文件。
2. **内存安全红线**: 禁止并行运行全量测试、全量测试与构建并行或其他会一次性加载全部模型/前端 worker 的高内存验证，以免内存爆炸。测试必须按文件或模块拆分，使用单进程、单 worker、严格串行的低内存方式分批执行；每批结束并释放资源后才能开始下一批，最终根据全部分批结果汇总验收。
3. 配置采用"服务级默认 + 用户级覆盖"双层模型：
   - 后端所有进程级配置统一由`core/global_config.py`管理（目录/路径,可控变量,可变常数,env），禁止在路由、业务、模型和工具中散落魔法数字; 
   - 用户级设置必须通过 `SettingsService` 读写并计算最终生效值，不得在零散的直接查询或修改用户设置表。新增用户设置时，必须同步完成数据库模型、Alembic 迁移、REST/gRPC DTO、前端设置调用和定向测试。
   - 读取端（API）必须遵循"**用户值 or 服务默认值**"的降级合并策略，写入端仅持久化用户显式覆盖的字段，禁止将服务默认值写入用户表。
4. **模型层**规范: 分为`models`和`schemas`两个模块,前者存放`XXXBase`(基础模型)和`XXX`(数据库模型),后者存放三个DTO(如:`XXXCreate`(创建DTO,继承`XXXBase`),`XXXUpdate`(更新DTO,继承`SQLModel`),`XXXOut`(响应DTO,继承`XXXBase`)).必要时可以适当变通.
5. **业务层**规范: 在业务层中,一切的业务函数都应该以其所属的业务领域划分到`service/<领域>/`中,即使函数非常小或者文件非常少.业务层必须依靠业务领域进行有效划分,以避免大规模业务重构.
6. REST **API层**规范: 所有 REST router 必须在 `api/rest/__init__.py` 集中注册，所有 REST Service 依赖必须通过 `api/rest/deps.py` 从当前请求绑定的 `ApplicationServices` 获取。
7. 应用生命周期统一管理：应用级 Service、调度器、线程池、文件监听器、模型运行时和 gRPC server 必须由 FastAPI lifespan 统一启动和关闭。新增长生命周期资源时，必须同时登记启动、关闭、异常记录和重复启动保护；禁止只创建线程、进程、连接池或监听器而不提供关闭路径。
8. 数据库统一管理：
   - 统一factory: 生产运行时只能通过 `core/db/engine.py` 获取统一 engine 和 Session factory。业务 Service 不得自行调用零散的 `create_engine`、维护独立连接池或在请求路径执行 `create_all`。
   - 数据库迁移规范: 数据库结构变化必须通过全局统一的 Alembic 迁移完成，并由 `core/db/migration.py` 统一执行版本检查、备份和升级；禁止在启动脚本、路由或临时脚本中直接补列、建表或注入业务数据。
9. 日志统一管理：后端模块统一使用 `logging.getLogger(__name__)`，日志格式、级别、控制台输出、文件轮转和模块级覆盖由日志配置统一管理，一般不要在业务代码中使用 print 记录运行状态。日志不得写入敏感信息(API Key、密码、Bearer Token、完整用户文档或未经脱敏的模型上下文)；异常日志必须保留操作类型、资源标识和可定位的错误信息。
10. **并发规则**: 任何并发任务、线程、协程、进程、服务、连接或分布式资源都必须明确唯一所有者、生命周期、状态机和释放责任，优先使用不可变数据、消息传递、任务队列和单写者模型，避免共享可变状态；必须使用锁时，只允许锁住最小的内存状态检查与原子更新，禁止在持锁期间执行 I/O、网络请求、数据库事务、进程调用、回调、事件派发、`await`、`join`或获取其他未知锁；同一系统的多锁必须规定并遵守唯一的全局获取顺序，禁止反向、嵌套和跨模块隐式加锁；所有等待必须有超时、取消和失败回退，所有任务必须在成功、失败、取消和超时后进入明确终态并在 `finally` 中释放资源，禁止永久停留在“运行中”状态；不同业务资源必须拥有独立任务、取消信号、错误边界和重试策略，一个资源失败不得阻塞或取消无关资源；跨服务操作不得依赖无限期分布式锁，应使用幂等请求、租约、版本号、事务或补偿机制；新增并发代码必须通过可控事件或屏障验证并发启动、乱序完成、单任务失败、超时、取消、重复执行和异常退出，无法说明所有权、锁顺序、终止条件与回收路径的并发实现不得合并。

## 部署运维
不得无关修改部署和打包文件。修改后端入口、端口、环境变量、运行时目录、默认资源、PyInstaller 配置、Electron 打包、安装器或容器配置时，必须同步检查开发环境与正式发布产物，并在当天变更历史中说明修改内容、原因和兼容性影响。